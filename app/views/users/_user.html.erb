<li>
  <%= gravatar_for user, size: Settings.users.size.max_length %>
  <%= link_to user.name, user %>

  <% if current_user.admin? && !current_user?(user) %>
  |  <%= link_to t(".btn_title"), user, method: :delete, data: {confirm: t(".confirm")} %>
  <% end %>
</li>
